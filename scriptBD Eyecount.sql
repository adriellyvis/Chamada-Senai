-- Arquivo-ref: scriptBD Eyecount.sql
-- Este arquivo foi substituído por arquivos organizados em /db
-- Estrutura: db/schema.sql  (DDL idempotente e com charset)
-- Seeds:     db/seeds.sql   (inserts iniciais idempotentes)
-- Leia db/README.md para instruções de execução.

-- Observação: para executar localmente:
-- mysql -u <usuario> -p < db/schema.sql
-- mysql -u <usuario> -p < db/seeds.sql
